# deepwiki-mcp

This repository is being rebuilt as a Rust-based MCP server.

## Layout

- `rust/`
  - new Rust source code for the MCP server
- `references/deepwiki-open/`
  - archived reference implementation of the previous Next.js + Python version
- `MCP_SERVER_PLAN.md`
  - migration and architecture plan

## Current status

The previous implementation has been moved under `references/deepwiki-open/` so the new Rust MCP server can be built in a clean workspace.
